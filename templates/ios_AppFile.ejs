app_identifier "<%= props.bundleId %>" # The bundle identifier of your app

team_id "<%= props.devTeamId %>" # Developer Portal Team ID
itc_team_id "<%= props.itcTeamId %>" # Get this by running: `bundle exec fastlane get_itc_team` from the `ios` folder of your react native app. It runs the lane defined in `ios/fastlane/FastFile`
